# Development Environment
# ========================

# Backend API URL (without trailing slash)
REACT_APP_API_URL=http://localhost:5003

# Environment
NODE_ENV=development

# API timeout in milliseconds (optional, default: 10000)
REACT_APP_API_TIMEOUT=10000

# Production Environment
# ========================
# Copy this file to .env.production and set these values:

# Backend API URL (without trailing slash)
# REACT_APP_API_URL=https://quiz-app-backend.vercel.app

# Environment
# NODE_ENV=production

# API timeout in milliseconds (optional, increase for slower connections)
# REACT_APP_API_TIMEOUT=30000

# Notes:
# - All variables must start with REACT_APP_
# - No quotes needed for values
# - No trailing slashes in URLs
# - Create separate .env.production file for production values
# - Restart dev server after changing values
