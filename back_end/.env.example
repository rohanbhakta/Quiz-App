# Development Environment
# ========================

# MongoDB connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname

# Server port
PORT=5003

# Environment
NODE_ENV=development

# JWT configuration
JWT_SECRET=your_development_secret_key

# Production Environment
# ========================
# Copy this file to .env.production and set these values:

# MongoDB connection string (use production cluster)
# MONGODB_URI=mongodb+srv://username:password@production-cluster.mongodb.net/dbname

# Environment
# NODE_ENV=production

# JWT configuration (use a strong, unique secret)
# JWT_SECRET=your_production_secret_key

# CORS configuration (deployed frontend URL)
# FRONTEND_URL=https://your-frontend-url.com

# Logging level (error, warn, info, debug)
# LOG_LEVEL=info

# Security Notes:
# - Use strong, unique secrets for JWT_SECRET in production
# - Never commit .env files to version control
# - Use different database credentials for development and production
# - Set appropriate CORS origins in production
# - Enable SSL/TLS for MongoDB connections
# - Use environment-specific configuration in deployment platform

# Deployment Notes:
# - Some platforms (like Vercel) will override PORT automatically
# - Set all environment variables in the deployment platform
# - Ensure MongoDB IP whitelist includes deployment platform IPs
# - Configure proper logging in production environment
# - Use appropriate security headers in production
